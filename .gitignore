# Build artifacts
/bin/
/dist/
coverage.out
coverage.html
/cortex

# Cortex local state (case files) — should also be ignored per-workspace by
# the .agent/.gitignore Cortex writes; listed here for this repo's own dogfooding.
/.agent/

# glyphrun run artifacts
/.glyphrun/

# VitePress
docs/.vitepress/dist/
docs/.vitepress/cache/
docs/.vercel/
node_modules/

# OS / editor
.DS_Store
*.swp
