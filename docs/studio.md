# Studio (removed)

The interactive Studio terminal board is not shipped in the CLI for now.

Use:

- [Quick Start](/quick-start) for human CLI workflows
- [For agents](/agents) for the agent guide
- [MCP server](/mcp) for assistant integration

Cross-repository session inspection is available through the CLI (`cortex sessions --json`, `cortex show <taskId> --json`) and the MCP server (`cortex serve`).
